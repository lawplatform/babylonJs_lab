import "./App.css";
import SceneContainer from "./components/SceneContainer";
import Monkey from "./world/obj/Monkey";
import "@babylonjs/loaders/glTF";
import Card_AB from "./components/Card_AB";
import anime from 'animejs/lib/anime.js';
import { observable } from "@legendapp/state"
import { Memo, For, Reactive, Show, Switch } from "@legendapp/state/react"
function App() {

	const view$ = observable(false);
	const textDiv = document.getElementById('card');
	view$.onChange(() => {
		console.log(view$.get());
	})

	let showMenu = anime({
		targets: '#mycard',
		opacity: [19, 100],
		translateX: 500,
		duration: 2000,
		easing: 'easeInOutQuart',
		complete: function() {
			console.log("animation end");
		},
		delay: 100,
	});
	const toggleShow = async () => {
		view$.set(!view$.get());
		showMenu.play();
		console.log("toggle show end");

	}
	const animationShow = async () => {
		showMenu.play();

	}
	return (
		<>
			<div id="my_canvas">
				hello world
				<button onClick={toggleShow} className="btn btn-neutral"> show ? </button>
				<button onClick={toggleShow} className="btn btn-neutral"> play animation </button>

				<div id="" className="">
					<Show if={view$}>
						{() =>
							<div className="nn w-xl bg-red-100" id="mycard">  htis is </div>
						}
					</Show>
				</div>
				<h1 className="text-3xl font-bold underline text-green-400">
					Hello world!
				</h1>
				<SceneContainer>
					<Monkey></Monkey>
					<ground name="ground" width={6} height={6} />
				</SceneContainer>
			</div >
		</>
	);
}

export default App;
